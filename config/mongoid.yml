development:
  sessions:
    default:
      database: cc_tJMJlABzVEcO
      hosts:
        - localhost:27017
      options:

development:
  sessions:
    default:
      database: maha_demo_development
      hosts:
        - localhost:27017
      options:

test:
  sessions:
    default:
      database: maha_demo_test
      hosts:
        - localhost:27017
      options:
        read: primary
        max_retries: 1
        retry_interval: 0
